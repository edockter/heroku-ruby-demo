<p id="notice"><%= notice %></p>

<% content_for(:pagetitle) do %>
  "<%= @category.label %>" Details
<% end %>

  <% content_for(:pagebuttons) do %>
      <div class="btn-group" style="margin-left: 2%; margin-bottom: 1%;">
          <%= link_to 'Edit', edit_category_path(@category), :class => "btn btn-sm btn-info" %>
          <%= link_to 'Back', categories_path, :class => "btn btn-sm btn-warning" %>
      </div>
  <% end %>

<p>
  <strong>Label:</strong>
  <%= @category.label %>
</p>

<br/>
